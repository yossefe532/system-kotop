name = "educore-pos-frontend"
pages_build_output_dir = "dist"

[vars]
# VITE_API_BASE_URL must be set in the Cloudflare Pages dashboard
# (Settings -> Environment variables) to your Render backend URL.
VITE_WALLET_LEDGER_ENABLED = "false"
VITE_ENABLE_DEMO_SEED = "false"
